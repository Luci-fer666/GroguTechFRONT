const carouselImages = [

...Array.from({ length: 39 }, (_, i) => ({
  img: `/assets/img/carrusel/Carrusel (${i + 1}).jpeg`
})),

...Array.from({ length: 5 }, (_, i) => ({
  img: `/assets/img/carrusel/Carrusel (${i + 1}).jpg`
}))

]

export default carouselImages